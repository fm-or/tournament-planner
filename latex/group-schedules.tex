\documentclass[
	a4paper,
	headinclude=false,
	footinclude=false,
	]{scrartcl}
    
\usepackage[margin=20mm,top=25mm, landscape]{geometry}

\usepackage[T1]{fontenc}% moderne Schriftkodierung
\usepackage[utf8]{inputenc}% Sonderzeichen vieler verschiedener Sprachen gleichzeitig (€, ° usw.)
\usepackage[english,main=ngerman]{babel}% Primär Deutsch und sekundär Englisch
\usepackage[autostyle]{csquotes}% Anführungszeichen mit \enquote{} passen zur Sprache von babel
\usepackage{lmodern}% Schriftfamilie latin modern basierend auf computer modern verträgt sich besser mit fontenc T1
\usepackage{microtype}% Verbessert den Textsatz durch z.B. geringfügige Buchstaben-Skalierungen

\renewcommand{\familydefault}{\sfdefault}
    
\usepackage{scrhack}% löst Probleme mit floats
\usepackage[pdftex]{graphicx}

\usepackage{tikz}
\usetikzlibrary{positioning,calc}

\usepackage{float}

\usepackage{tournamentstyle}

\usepackage{datatool}% load data from csv files

\usepackage{xstring}% \IfSubStr

\usepackage{ifthen}

\def\b{5.5cm} % width of box
\def\c{5cm} % text width
\def\d{7cm} % text width
\tikzset{titlenode/.style={draw, minimum width=\b, minimum height=0.8cm, align=left}}
\tikzset{itemnode/.style={titlenode, text width=\c}}
\tikzset{itemnodeWide/.style={titlenode, text width=\d}}
\tikzset{smallnode/.style={draw, minimum height=0.8cm, minimum width= 2cm}}

\newcommand{\createGame}[8]{%
  \node[smallnode, #1] (G#2) {\strut #7};
    \node[smallnode, right=0mm of G#2] (G#2court) {\strut Court #5};
  \node[itemnode, right=0mm of G#2court] (G#2serve) {\strut #3};
  \node[itemnode,  right=0mm of G#2serve] (G#2receive) {\strut #4};
  \node[itemnode, right=0mm of G#2receive] (G#2referee) {\strut #6};
 % \node[smallnode, text width=16mm, align =center, right=0mm of G#2referee] (G#2info) {\strut \ifthenelse{\a=1}{#8}{#7}};
}


\DTLloaddb{scheduledata}{schedule.csv}% load the schedule data from csv

\DTLloaddb{groupdata}{groups.csv} % load group data


\begin{document}
\makeatletter
\let\teamNames\empty

\DTLforeach{groupdata}{\group=Group,\team=Team}{%
  \IfSubStr{\teamNames}{\team}{}{% if not present
    \ifx\teamNames\empty
      \edef\teamNames{\team}%
    \else
      \edef\teamNames{\teamNames,\team}%
    \fi
  }%
}
\makeatother


\newcounter{spielzaehler}

\foreach \team in \teamNames {%

\setcounter{spielzaehler}{0}

  \begin{center}
		\huge{\textbf{\tournamentname}}\\
		\vspace{1.25em}
		\LARGE{\textbf{Schedule of \team}}\\
\end{center}

\begin{figure}[H]
 \centering
   \begin{tikzpicture}
   \def\smallgap{5mm}
   \def\mediumgap{10mm}
   \def\largegap{15mm}
        	
   \coordinate (origin) at (0,0);

\createGame{below=0mm of origin}{Nr}{Serving}{Receiving}{}{Referee}{Time}{Points}% this is the header


\DTLforeach{scheduledata}{\match=Match Nr,\court=Court,\serving=Team 1,\receiving=Team 2,\referee=Referee,\time=Time}
{% loop over the rows
  \ifthenelse{\equal{\serving}{\team} \OR \equal{\receiving}{\team} \OR \equal{\referee}{\team}}{%
  
\stepcounter{spielzaehler} % zuerst hochzählen

 \ifnum\value{spielzaehler}>1
   \expandafter\createGame
    \expandafter{below=0mm of G\number\numexpr\value{spielzaehler}-1\relax}{\arabic{spielzaehler}}{\serving}{\receiving}{\court}{\referee}{\time}{:}%
\else
    \expandafter\createGame
    \expandafter{below=0mm of GNr}{\arabic{spielzaehler}}{\serving}{\receiving}{\court}{\referee}{\time}{:}%
\fi

  }{%
    % nichts oder anderes%
  }%
}%


\end{tikzpicture}
\end{figure}
    
\newpage
}

\end{document}
